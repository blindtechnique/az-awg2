# Changelog

Все заметные изменения проекта. Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — по [SemVer](https://semver.org/lang/ru/).

## [3.0.0-beta.29] — 2026-08-31 · ветка `beta`

### Добавлено

- **Сверка `<iface>.env` — копии, которую читает датапас (`check_iface_env`).** Последнее незакрытое ребро матрицы, и оно оказалось шире, чем числилось: файл несёт не только подсеть, а сразу несколько значений, у каждого свой источник в `services.env` — `SUBNET`, `PORT`, а в awg3 ещё `MTU` и `WAN`. Пишет его установщик, читает датапас, поднимая интерфейс. Если копия разошлась, интерфейс встанет по **ней**, а весь остальной проект будет считать иначе, и не скажет никто.

  Уровни — по тому, что расхождение ломает:

  | поле | уровень | что происходит |
  |---|---|---|
  | `SUBNET` при `NAT=1` | `bad` | MASQUERADE встаёт на чужой диапазон: клиенты подключаются, интернета не видят |
  | `SUBNET` при `NAT=0` | `warn` | за NAT отвечает ваниль, копия пока инертна — но устарела |
  | `PORT` | `bad` | интерфейс слушает не там, куда стучатся выданные конфиги |
  | `WAN` | `bad` | MASQUERADE на чужом выходе — трафик наружу не пойдёт |
  | `MTU` | `warn` | интерфейс с чужим MTU: крупные пакеты пропадают |

  Молчит, когда файла нет (датапас тогда и не работает — об этом говорит `check_iface`) и когда поля в файле нет вовсе: в az в этом файле не бывает ни `MTU`, ни `WAN`, и требовать их было бы ложной тревогой.

  Проверка покрытия вызовов обобщена: теперь она требует, чтобы **каждая** пер-интерфейсная сверка (`check_live`, `check_iface_env`) вызывалась столько же раз, сколько `check_iface`.

  Этим закрыты все рёбра матрицы согласованности, признанные незакрытыми.

## [3.0.0-beta.28] — 2026-08-31 · ветка `beta`

### Добавлено

- **Сверка живого интерфейса с серверным конфигом (`check_live`).** Последнее ребро матрицы, где сравниваются не два файла, а файл и **ядро**. `check_peers` доказывает согласие клиентских конфигов с серверным — то есть всё на диске. Но между конфигом и тем, что реально загружено в интерфейс, есть зазор, и он самый коварный: на диске согласовано, диагностика зелёная, а сервер работает по прежнему состоянию. Так выглядит «конфиг переписали, а `awg setconf` не сделали» — ровно то, что остаётся после восстановления, убитого между копированием файлов и перезапуском сервисов.

  Две сверки по одному интерфейсу, уровни по доказуемости:

  | что нашли | уровень | почему |
  |---|---|---|
  | публичный ключ интерфейса ≠ выведенный из конфига | `bad` | интерфейс поднят из другого файла; клиенты по нему не сойдутся, законного состояния нет |
  | пир есть в конфиге, но не загружен в ядро | `bad` | этот клиент не подключится до перезапуска |
  | пир загружен в ядро, но его нет в конфиге | `warn` | доступ переживёт только до перезапуска, но пир мог быть добавлен руками через `awg set` |

  Молчит там, где доказать нечем: интерфейс не поднят (об этом уже говорит `check_iface`), нет `awg`, пустой `PrivateKey`.

  Проверка покрытия вызовов расширена под другую ось: `check_live` идёт по **интерфейсам**, а не по каталогам клиентов, поэтому она связана с `check_iface` — сколько интерфейсов осматривается, столько же раз надо спросить и живое ядро.

## [3.0.0-beta.27] — 2026-08-31 · ветка `beta`

### Исправлено

- **Подсети слоя 3.0 не закреплялись, хотя обоснование закрепления написано двумя строками выше.** В `plan_services` стоит комментарий: «Подсети закрепляем так же, как порты: от них зависят Address и AllowedIPs всех выданных конфигов. Пересчёт из конфига ванили на каждом прогоне означал бы, что её переезд на другой диапазон уводит сервер, а у клиентов остаётся прежний адрес — regen-all это не чинит, он адреса не трогает». Он применён к `AZ_SUBNET`/`VPN_SUBNET` и **не применён** к `AZ3_SUBNET`/`VPN3_SUBNET`, которые пересчитывались от ванили при каждом прогоне.

  Измерено на стенде: при закреплённых `2.0 = 10.29.9` и `3.0 = 10.29.10` и «переехавшей» ванили (`az_base = 10.29.99`) слой 2.0 остаётся на `10.29.9`, а слой 3.0 уезжает на `10.29.101` — при том, что у всех выданных клиентов остаётся прежний `Address`.

  Достижимо не только настоящим переездом ванили. `_subnet_of` при нечитаемом её конфиге отдаёт **запасное** значение, поэтому один прогон, в котором конфиг ванили временно недоступен, навсегда сдвигал подсети слоя 3.0. Слой 2.0 от этого был защищён закреплением, слой 3.0 — нет.

### Изменено

- **Тест закрепления подсетей покрывает оба слоя.** Он существовал, но его срез обрывался на `VPN_SUBNET=` — двумя строками раньше слоя 3.0, ровно там, где закрепления и не было. Теперь срез идёт до `VPN3_SUBNET=`, и проверяются обе подсети 3.0: и что закреплённые переживают переезд ванили, и что на первой установке они по-прежнему вычисляются от неё.

## [3.0.0-beta.26] — 2026-08-31 · ветка `beta`

### Добавлено

- **MTU у выданных конфигов сверяется с объявленным (`check_client_mtu`).** Ребро того же вида, что порт и хост, и такое же непроверявшееся: `MTU` живёт в `services.env` и в каждом клиентском конфиге, а `regen-all` его **не меняет** — он правит только строки обфускации, сохраняя ключи, IP и peer. Значит после смены MTU все выданные конфиги молча остаются на прежнем.

  Вред тише, чем у порта, и оттого противнее: соединение устанавливается, мелкие запросы ходят, а всё крупное встаёт — «интернет работает, но сайты не грузятся». Уровень `warn`: расхождение доказано двумя файлами, но у него есть законное состояние — MTU только что сменили, конфиги ещё не роздали.

  Проверка покрытия вызовов, добавленная в прошлой версии, потребовала новую функцию во всех каталогах автоматически — ровно ради этого она и писалась.

## [3.0.0-beta.25] — 2026-08-31 · ветка `beta`

### Добавлено

- **Проверка того, что проверки вообще вызваны.** Последнее незакрытое ребро матрицы согласованности — и самое неприятное по природе: стенды вырезают функции диагностики `sed`-ом и гоняют их на своих синтетических каталогах, поэтому **удаление любой строки вызова оставляло весь набор зелёным**. Инвариант для одного каталога тихо исчезал, и глазами это тоже не видно: каталог продолжают осматривать соседние функции.

  Теперь набор требует, чтобы каждая из `check_ports`, `check_client_hosts` и `check_peers` была вызвана для **каждого** каталога клиентов. Список каталогов берётся из `regen_all` в client-скрипте, а не из самой диагностики: иначе проверка сверяла бы её с собой. Отдельно ловится обратная опечатка — вызов на каталог, которого проект не создаёт.

  Измерено в обе стороны: без этого раздела удалённая строка вызова проходит зелёной, с ним — роняет весь набор с точным сообщением, какая проверка для какого каталога пропала.

## [3.0.0-beta.24] — 2026-08-31 · ветка `beta`

### Исправлено

- **Осмотр клиентских конфигов выключался ровно там, где поломка гарантирована.** `check_ports` начиналась с раннего выхода: `[ -n "$want" ] && [ "$want" != 0 ] || { warn "порт не объявлен"; return; }` — и при пустом или нулевом объявленном порте не заглядывала в каталог клиентов вовсе. А именно в этом состоянии клиенты и выписываются негодными:

  - слой 3.0: `PORT="${AZ3_PORT:-0}"` → `Endpoint = host:0`, недействителен всегда;
  - слой 2.0 в az: `PORT="${AZ_PORT:-51443}"` → `Endpoint` на **ListenPort ванильного WireGuard**. Порт правдоподобный, глазом не отличить.

  Порт вне 1..65535 доказывается **одним** клиентским файлом — объявленное значение для этого не нужно. Поэтому осмотр (`check_client_ports`) идёт теперь до всякого раннего выхода.

- **Отсутствие порта в `services.env` перестало быть одним диагнозом на два случая.** Переменной нет в файле вовсе — `warn`: так бывает на установке прежних версий, и доказать по этому нечего. Переменная есть, но пуста или ноль — `bad`: слой объявлен включённым, порта у него нет, и каждый следующий выданный клиент получает негодный `Endpoint`.

- **`check_iface` больше не кричит «порт 0 не слушается».** Этот `bad` недоказуем из файлов — он требует поднятого интерфейса и живого `ss`, — и уводит владельца перезапускать сервис, тогда как чинить надо объявление порта и переиздание конфигов. Про недействительный порт теперь говорит `check_ports`, и говорит точнее.

### Изменено

- **`tests/test_ports.sh` выражает новый контракт.** Прежний закреплял ранний выход как правильное поведение. Разделены случаи «переменной нет» и «переменная пуста», добавлены проверки, что негодный порт у клиентов виден **без** объявленного значения и что исправные конфиги при этом не оговариваются.

## [3.0.0-beta.23] — 2026-08-31 · ветка `beta`

### Исправлено

- **Понижение профиля обфускации 3.0 клало слой целиком — и диагностика показывала это зелёным.** Самый разрушительный исход во всей матрице согласованности, и сигнал был ровно противоположным.

  `apply_to_server` писал `<iface>.v3` только `if [ -n "$V3_BLOCK" ]` — при пустом блоке файл **не переписывался и не удалялся**. Пустым блок становится штатно: пресеты `router` и `low` объявлены без header protection. Значит `medium → router` (обычная операция, без единой тихой ошибки) оставлял прежний `.v3`, датапас применял его на `ExecStartPost`, сервер продолжал ждать защиту заголовка, а клиентские конфиги уже перевыпущены без неё — не подключался **никто**.

  Доктор это не ловил, а подтверждал: ветка считала «ключ на интерфейсе есть» безусловно хорошим и печатала «header protection применена (пресет router)». Именно `router` её иметь и не должен.

  Теперь `apply_to_server` при пустом профиле **убирает** прежний `.v3` и говорит об этом. А доктор судит по **профилю** (`AWG_HPK_HEX` в `obfuscation3.env`), а не по имени пресета: имя — ярлык в `.meta`, а клиентские конфиги выписываются по профилю. Решение принимается тем же условием, что у генератора, — иначе доктор и генератор разошлись бы в понимании одного файла. Четыре исхода вместо двух:

  | профиль объявляет | на интерфейсе | вердикт |
  |---|---|---|
  | да | есть | `ok` |
  | да | нет | `warn` — применить профиль заново |
  | **нет** | **есть** | **`bad`** — застрявший `.v3`, клиенты выданы без защиты |
  | нет | нет | `ok`, так и задумано |

- **`check_v3_stale`** — та же сверка файла с файлом: `.v3` содержит `header_protection_key`, а профиль его не объявляет. Работает и тогда, когда демона спросить нечем, и ловит застрявший файл **до** перезапуска датапаса.

### Изменено

- **`tests/test_doctor_v3.sh` выражает новый контракт.** Прежний набор закреплял суждение по имени пресета — то самое, из-за которого разрушительный исход был зелёным. Теперь профиль стал отдельным измерением стенда, добавлен случай «ключ есть, профиль не объявляет», а извлечение блока опирается на именованный конец, а не на счёт закрывающих `fi`: их число меняется от любой правки внутри.

## [3.0.0-beta.22] — 2026-08-31 · ветка `beta`

### Исправлено

- **Объяснения в диагностике не печатались никогда.** `bad()` и `warn()` принимают второй аргумент — что именно случилось и что делать, — и молча его выбрасывали: печатался только первый. Таких вызовов пять в каждом докторе, и написаны они ровно ради того, чтобы владелец понял, куда идти: «клиенты стучатся по одному порту, сервер слушает другой», «этим клиентам надо раздать конфиги заново». Теперь объяснение идёт отдельной строкой в терминал и приклеивается к той же строке отчёта для бота — новый статус сломал бы разметку, которую бот разбирает по трём известным префиксам.

### Добавлено

- **Сверка ключей и пиров (`check_peers`).** Приватный ключ клиента существует **только** в его конфиге, публичный — в `[Peer]` серверного. Пара пишется не атомарно: `add_client` делает три отдельные записи (клиентский файл, `awg set` на живой интерфейс, `[Peer]` в конфиг), `del_client` — две в обратном порядке. Значит расхождение достижимо обрывом, а не только переносом сервера; а восстановление из архива, снятого до починки `DEST`, даёт худший вариант — серверный конфиг с пирами и пустой каталог клиентов.

  Уровни назначены по доказуемости, и это главное в проверке:

  | что нашли | уровень | почему |
  |---|---|---|
  | ключа клиента нет среди пиров | `bad` | клиент выдан, но сервер его не примет; законного состояния нет |
  | у клиента чужой ключ сервера | `bad` | handshake невозможен ни при каких настройках |
  | два клиента делят адрес | `bad` | `next_ip` считает адрес свободным и отдаст его третьему, забрав маршрут у работающего |
  | пир без клиентского файла | `warn` | доступ есть, отозвать по имени нечем — но пир мог быть заведён руками |
  | нет `awg` или пустой ключ | молчим | вывести публичный ключ нечем, доказать нечего |

  Сверка идёт по **ключу**, а не по адресу: `next_ip` переиспользует освободившиеся адреса, и сверка по `AllowedIPs` дала бы ложное зелёное.

- **`tests/test_consistency.sh`** — по каждому уровню отдельный раздел, включая те, где проверка обязана **молчать**: нет `awg`, чужой файл в каталоге, пустой приватный ключ.

## [3.0.0-beta.21] — 2026-08-30 · ветка `beta`

### Исправлено

- **Наборы под пространством имён в CI молча пропускались.** Ubuntu 24.04 запрещает непривилегированные user namespaces через apparmor, и на `ubuntu-latest` `unshare -Urm` недоступен. Набор в этом случае пропускает свою главную часть и всё равно выходит с нулём.

  Аннотация о пропуске была написана специально ради этого — но её съедал сам прогон: `run()` захватывает stdout набора и переиздаёт аннотацию **только при падении**. То есть «аннотаций ноль» ничего не доказывало, а выглядело доказательством. Ровно та ложная зелень, против которой писались сами наборы.

  Теперь `::notice` из вывода переиздаётся всегда, а в CI добавлен шаг, разрешающий namespaces, — и он же печатает, получилось ли. Проверено на ветке `main`, где набор запускается шагом напрямую: до шага аннотация о пропуске приходила, после — исчезла, то есть настоящий прогон действительно пошёл.

## [3.0.0-beta.20] — 2026-08-30 · ветка `beta`

### Исправлено

- **За NAT в `Endpoint` клиента уезжал приватный адрес.** `server_host()` при отсутствии `WIREGUARD_HOST` угадывает адрес через `ip route get`, и результат не фильтровался: на машине за NAT в конфиг попадал `10.x`/`172.16-31.x`/`192.168.x`. Клиент назывался созданным и не подключался ниоткуда. Измерено на стенде: было `10.0.0.5`, стало `203.0.113.7` — угаданный приватный адрес отбрасывается, и берётся внешний.

  Фильтруется **только угаданное**. Объявленный `WIREGUARD_HOST` не оспаривается никогда: `192.168.1.10` у домашнего сервера в своей же локалке законен, и подмена его на внешний всё бы сломала.

### Изменено

- **`$AWG_DIR/server_host` помечен как мёртвый.** Его пишет `resolve_host()` и кладёт в архив `awg-backup.sh`, но не читает никто: и `client-awg.sh`, и бот берут `WIREGUARD_HOST` из живого `/root/antizapret/setup`. Полагаться на него нельзя ещё и потому, что после переноса он содержит адрес прежней машины — сверка читала бы устаревшее против устаревшего и молчала бы ровно тогда, когда нужна.

### Добавлено

- **Доктор сверяет хост в выданных конфигах с объявленным адресом.** `check_ports` сверял у `Endpoint` только порт — хост не сверялся нигде, хотя пишется он ровно в одном месте (`add_client`) и всегда из единственного объявленного значения. Значит несовпадение — всегда настоящее расхождение, и сверка эта не ходит в сеть: файл против файла.

  Это `warn`, а не `bad`: законные причины есть (смена объявленного хоста оставляет выданные конфиги на прежнем), причина неизвестна, а лечение у всех одно — поэтому формулировка «выданы на другой адрес», а не «сервер переехал». Единственный `bad` — `Endpoint` без разбираемого хоста: доказывается одним файлом и означает конфиг, который не подключится никогда. В подсказке сказано прямо, что **`regen-all` адрес не меняет** — он правит только строки обфускации, сохраняя ключи, IP и peer.

- **`tests/test_endpoint.sh`** — разделение по доказуемости, которое и есть суть правки: что проверяется одним файлом, что двумя, а что за NAT не проверяется в принципе. Отдельно измеряется, что доктор **молчит** на исправном сервере: за NAT, при round-robin, при плавающем адресе и на голом IP.

## [3.0.0-beta.19] — 2026-08-30 · ветка `beta`

### Добавлено

- **Два правила про `exec` в `tests/audit_shell_patterns.py`.** Обе ловушки, из-за которых они появились, я внёс сам в предыдущей правке, и поймал их стенд — не чтение кода и не аудит. Значит третья такая прошла бы тем же путём.

  `exec-unbraced` — голый `exec N>файл`. У `exec` без команды неудачная перенаправка завершает неинтерактивную оболочку **целиком**: ни `|| return`, ни `|| true`, ни `set +e` до неё не доходят. Ограждением считаются и `{`, и `(`.

  `exec-redirect-persists` — `2>` на самом `exec`. Перенаправления такого `exec` применяются к оболочке и остаются навсегда: `exec 9>&- 2>/dev/null` тихо уводит в `/dev/null` весь дальнейший `stderr` скрипта. Здесь ограждает **только** `(`: фигурная группа подоболочки не создаёт, и внутри неё перенаправление ложится на текущую оболочку точно так же. Безопасная форма — `{ exec 9>&-; } 2>/dev/null`, где между `exec` и `2>` стоит закрывающая скобка.

  Правила проверены на девяти формах — трёх плохих и шести хороших, включая штатную проверку терминала `(exec </dev/tty) 2>/dev/null`, которую пропускать обязательно.

### Изменено

- **Отказ снимка `stats.db` больше не роняет весь бэкап.** Бот отдаёт архив только при нулевом коде, и ронять выдачу из-за потерянных счётчиков значило бы менять мелкую потерю на отсутствие бэкапа вообще. Про отказ по-прежнему сказано вслух. Клиентские ключи — другое дело: они невосстановимы, и там код остаётся ненулевым.

## [3.0.0-beta.18] — 2026-08-30 · ветка `beta`

### Исправлено

- **`stats.db` уезжал в архив пустой оболочкой.** База открыта в режиме WAL (`PRAGMA journal_mode=WAL`), поэтому свежие записи живут в `stats.db-wal`, а сам файл базы остаётся заголовком в 4 КиБ. Бэкап копировал один только `stats.db` — измерено: такая копия открывается с **`no such table`**, то есть теряется не часть статистики, а вся схема.

  Снимок теперь берётся через backup API sqlite (`Connection.backup`) системным `python3` — он корректен при живом писателе и даёт один самодостаточный файл. Именно системным, а не из venv: venv создаётся только вместе с ботом, а `sqlite3` входит в стандартную библиотеку; CLI `sqlite3` в зависимостях не объявлен ни в одном репозитории.

- **Восстановление отдавало данные ПРЕЖНЕЙ установки.** `stats.db` клался поверх, а осиротевшие `stats.db-wal` и `stats.db-shm` оставались рядом — и sqlite при следующем открытии проигрывал старый журнал поверх положенного файла. Измерено: архив на 7 записей, после восстановления читается 500 прежних, первая строка `прежний0`. Ни одна команда при этом не возвращала ошибку. Журналы теперь убираются — но только под замком, из-под живого поллера этого делать нельзя.

### Добавлено

- **Один замок на состояние слоя (`flock`).** Под ним серверные конфиги, каталог клиентов, `expiry.tsv` и `stats.db`. Берётся внутри самих скриптов, а не в юнитах, — тогда под него попадают сразу все входы: бот, меню, ssh и оба таймера (`awg-stats` раз в минуту, `awg-expire` раз в пять). До этого восстановление переписывало всё состояние, пока `expire_check` в том же каталоге делал `mv` поверх `expiry.tsv`, а `del_client` правил серверный конфиг.

  Ручные операции ждут (`-w`), таймеры не ждут ни секунды (`-n`) и пропускают тик: у oneshot-юнитов `TimeoutStartSec` по умолчанию 90 секунд, и ожидание кончилось бы SIGTERM посреди правки файлов. Пропуск тика возвращает **ноль** — это не отказ. Читающие команды (`list`, `overview`) оставлены без замка сознательно, иначе бот вис бы на всё время восстановления.

  Файл замка — в `/run`. Не в `/tmp`: у бота в юните `PrivateTmp=true`, его `/tmp` отдельный, и замок там не сериализовал бы ничего, при этом выглядя рабочим. И не в каталоге слоя: его переписывает само восстановление, а смена inode у файла замка означала бы две стороны, держащие разные замки, без единого признака беды.

  Замок отпускается **до** перезапуска сервисов. Это не косметика: `doall.sh` через ванильный `custom-up.sh` запускает `awg-reintegrate.sh` фоном, и фоновый потомок унаследовал бы дескриптор, держа замок всю свою жизнь — таймеры молча пропускали бы тики, и виновника не было бы видно ничем.

- **Разделы про замок и про WAL в `tests/test_restore_integrity.sh`** — на настоящих `backup` и `restore` под отдельным пространством имён. Измеряется: занятый замок заставляет `expire-check` и `poll` пропустить тик с нулевым кодом, а восстановление — не начаться вовсе (ни метки, ни правок; в конфиг для этого кладётся часовой, иначе проверка проходила бы и при совпадении файла с архивным). Живая база с непустым `-wal` переживает круг «бэкап → восстановление» и читается как архивная, а не как прежняя.

## [3.0.0-beta.17] — 2026-08-30 · ветка `beta`

### Исправлено

- **Бэкап никогда не содержал клиентских приватных ключей.** `awg-backup.sh` объявлял `DEST="$AZ/awg"`, то есть `/root/antizapret/awg` — каталог, которого не создаёт никто и на который нет симлинка; весь остальной репозиторий (`install.sh`, `antizapret-awg-integration.sh`, `awg-doctor.sh`, `awg_stats.py`, `client-awg.sh`) знает только `/opt/antizapret-awg`.

  Поэтому `[ -d "$DEST/clients" ]` был **всегда** ложен, и в архив не попадали ни клиентские конфиги, ни `stats.db`, ни `expiry.tsv` — молча, потому что рядом стоял `|| true`. Восстановление клало `awgstate/*` обратно в тот же мёртвый путь. Архив выглядел полным и не содержал главного: приватные ключи клиентов существуют **только** в `clients/`, в серверном конфиге лежат одни публичные, переиздать их нечем.

  Обратный тест на прежнем файле показывает это дословно: «в архиве 0 клиентских конфигов из 4», «stats.db не попал в архив», «expiry.tsv не попал в архив», а после восстановления — «восстановлено 3 клиентских конфигов из 4», удалённый клиент не вернулся. В `awg3` путь везде один и тот же, там этого не было: расхождение доехало до одного репозитория.

  Раздел 1 нового набора сторожит теперь именно **согласие файлов между собой**, а не совпадение с константой — расхождение и было дефектом.

- **Повтор оборванного восстановления мог снести уцелевший PKI.** Подмена `/etc/openvpn/easyrsa3` идёт через `.restore`/`.old`, и смерть между двумя переименованиями оставляет каталог только в `.old`. Повторный прогон начинался с `rm -rf …easyrsa3.old` — то есть удалял единственную копию раньше, чем ставил на место новую. Теперь брошенный `.old` возвращается на место до того, как что-либо удаляется, и об этом печатается строка.

### Исправлено

- **Восстановление не могло сообщить об отказе.** После распаковки каждый шаг шёл с `|| true` либо `|| err`, а `err` — это `printf`, он возвращает 0; последней строкой стоял `return 0`. Значит `do_restore` возвращал ноль всегда: и когда не поднялся ни один тоннель, и когда не выполнилось ни одного `systemctl` вообще (без `services.env` оба флага слоёв берут умолчание 0, и блок перезапуска становится пустым). Бот печатает «✅ Восстановлено.» ровно по нулевому коду — эта галочка приходила и на мёртвом сервере.

  Теперь код накапливается и возвращается в конце, после того как всё сказано; прогон по-прежнему не обрывается посередине — файлы уже подменены, аварийный выход сделал бы хуже. С перезапусков снят `2>/dev/null`: бот показывает stderr только при ненулевом коде, так что раньше причина от systemd не доходила до владельца никогда, даже когда `err` её печатал.

### Добавлено

- **Метка `.restore-in-progress` — по образцу `.migrate-in-progress`.** Ставится до первой разрушающей записи, снимается только если сервисы поднялись. Она закрывает границу «файлы новые, сервисы старые»: смерть между копированием и перезапуском оставляет систему в состоянии, где на диске всё согласовано (доктор видит норму), а работает сервер по прежней конфигурации. Метка переживает перезагрузку и оборванный ssh и несёт путь архива — есть чем повторить.

- **`awg-doctor` проверяет метки — и `.restore-in-progress`, и `.migrate-in-progress`.** Проверка стоит **до** требования `services.env`: восстановление, убитое до копирования этого файла, иначе выглядело бы как «не установлено», и объяснение не показалось бы никогда. Это канал для владельца, который вывод прогона не читал.

- **Бэкап пересчитывает то, что уехало в архив.** `[ -d "$DEST/clients" ] && cp … || true` гасит и промах условия, и отказ копирования; только счёт отличает «клиентов нет» от «клиенты есть, но в архив не попали». Неполный архив теперь даёт ненулевой код, и бот такой файл в чат не отправляет.

- **Восстановление отличает пустой архив от отказа копирования** и говорит об этом прямо, а не молчит.

- **`tests/test_restore_integrity.sh`** — настоящие `backup` и `restore` под отдельным пространством имён: содержимое архива, прерывание на границе «файлы новые, сервисы старые», отказ перезапуска, успешный прогон, архив без клиентских ключей. Плюс статическая часть, работающая и там, где `unshare` недоступен: инвариант каталога слоя, накопитель кода возврата, порядок проверки метки в докторе.

## [3.0.0-beta.16] — 2026-08-30 · ветка `beta`

### Исправлено

- **Самый разрушительный отказ прогона был единственным полностью заглушённым.** Пересоздание клиентских конфигов стоит последним шагом: сервер к этому моменту уже переехал на новый профиль обфускации. Строка выглядела как `regen-all 2>/dev/null || true` — вывод в никуда, код съеден. Если пересоздание не отрабатывало, не подключался **никто**, а прогон допечатывал «Готово» и выходил с нулём.

  Теперь отказ печатается вместе с тем, что именно случилось и что делать, а прогон возвращает **ненулевой код** — уже после того, как всё сказано и подсказки выведены. Обрывать прогон на этом месте нельзя: сервер настроен, и аварийный выход сделал бы хуже. Но «не подключится никто» с кодом 0 автоматика читает как успех, и это ровно та зелень поверх сломанного, против которой всё и затевалось.

### Добавлено

- **Прерывание `--reconfigure` на самой опасной границе (`tests/test_reconfigure_interrupt.sh`).** Порядок в прогоне такой: сначала новый профиль и переезд **сервера**, и только последним шагом — клиентские конфиги. Смерть между этими действиями означает не «часть клиентов отстала», а «не подключается никто».

  Граница ловится точно: `md5sum` во всём проекте зовётся только внутри `regen_all`, по разу на клиента в начале итерации, — значит убийство на первом её вызове и есть «сервер переехал, клиентов не тронули». Запускается весь настоящий скрипт в отдельном пространстве имён, как и в наборе о миграции.

  Измерено: профиль сервера сменился (`Jc 4 → 8`), серверный конфиг обновлён, **все три** клиентских конфига остались на старом профиле — и прогон об этом сказал и вернул ненулевой код. Восстановление проверено отдельно: повторный прогон **без** `--reconfigure` переприменяет существующий профиль и доводит клиентов до него, не перевыпуская профиль заново и не трогая ключи. Именно без — с `--reconfigure` владелец получил бы третий профиль и сломал клиентов ещё раз.

## [3.0.0-beta.15] — 2026-08-30 · ветка `beta`

### Добавлено

- **Прерывание НАСТОЯЩЕЙ миграции (`tests/test_migrate_interrupt.sh`).** До сих пор проверка миграции держалась на воспроизведении состояния «после падения» и на чтении кода. Теперь запускается весь `patches/antizapret-awg-integration.sh --migrate` целиком — в отдельном пространстве имён (`unshare -Urm --map-root-user`), с `tmpfs` поверх `/etc`, `/root`, `/opt` и `/usr/local`. За пределами пространства имён не меняется ничего, и сам скрипт не правится ни на строку: `install_awg` сам выходит рано, увидев заглушки `awg` и `modinfo`.

  Прерывание точечное: заглушка `mv` выполняет переименование по-настоящему и убивает процесс сразу после переезда **первого** сервиса. Это и есть та граница, где первый конфиг уже переименован, второй ещё нет, а `MODE=parallel` записан двумя шагами раньше. Измеренный результат: прогон убит с кодом 137, состояние ровно посередине, метка на месте — а повторный запуск распознаёт незавершённую операцию, доводит второй сервис и снимает метку.

  Проверено на покраснение: со снятой меткой повтор снова коротит, второй сервис не переезжает никогда, и — показательно — ловится это **инвариантом портов**: «объявлен 33001, в конфиге 52443».

  Две подробности стенда, без которых он мерил бы не то. Пустой `tmpfs` поверх `/etc` ломает половину утилит: в Debian `awk` и `sed` ходят через `/etc/alternatives`, поэтому настоящий `/etc` сначала подкладывается bind-монтированием и копируется в `tmpfs`. И заглушка `shuf` обязана выдавать **разные** порты: `pick_random_port` зовут дважды, и второй вызов получает первый порт в списке исключений.

## [3.0.0-beta.14] — 2026-08-30 · ветка `beta`

### Добавлено

- **Инвариант портов в диагностике.** Порт живёт в трёх местах сразу: в `services.env` (объявленный), в серверном конфиге (`ListenPort`) и в каждом выданном клиенте (`Endpoint`). Разъехаться они могут молча — правка руками, оборванная миграция, перенос сервера, — и до сих пор этого не сверял ни один инструмент. Доктор краснел лишь косвенно, «порт N не слушается», и то если интерфейс вообще поднялся.

  Теперь `awg-doctor` сверяет все три и говорит, что именно разошлось: конфиг с объявленным — числами, клиенты — числом отставших из общего и примером имени. Разница существенна на практике: расхождение у клиентов лечится **переизданием конфигов**, а не перезапуском сервиса, и раньше это было неотличимо от «сервис лежит».

  `tests/test_ports.sh` проверяет шесть состояний: всё согласовано, разошёлся серверный конфиг, все клиенты на старом порту, отстал один из четырёх, порт не объявлен вовсе, в конфиге нет `ListenPort`. Функция вырезается из настоящего доктора и исполняется как есть.

## [3.0.0-beta.13] — 2026-08-30 · ветка `beta`

### Исправлено

- **Восстановление уничтожало ванильный PKI, если умереть посередине.** Единственный шаг `do_restore`, который делает состояние **хуже** исходного, а не просто неполным: `rm -rf /etc/openvpn/easyrsa3` и только потом `cp -r` из архива. Смерть между этими действиями оставляла систему вообще без PKI, и взять его было уже неоткуда — в архиве он есть, но восстановление до него не дошло. Теперь новый каталог кладётся рядом и подменяет оригинал переименованием: окно сузилось до одного `rename`.

- **Метка незавершённой миграции прикрывала не всё.** Вчерашняя правка хранила только режим, а имена старых интерфейсов повтор читал из **уже перезаписанного** `services.env` — значит `old_if` совпадал с `new_if`, ветка переименования пропускалась целиком, и прогон всё равно доходил до «✅ Миграция завершена». Метка теперь хранит и имена, и подсети, а повтор берёт их именно оттуда.

  Второй пробел там же: `install.sh` про метку не знал ни строкой и коротил на собственной проверке `MODE`, поэтому штатный путь `install.sh --migrate` до починки не доходил вовсе. Теперь проверяет.

### Добавлено

- **Восстановление из бэкапа покрыто тестом на прерывание** — тем же способом, что и в `awg3`: заглушка `cp` убивает процесс на N-м вызове, потому что отказ команды операция бы проглотила (`|| true`). Плюс два пункта на метку миграции: что она хранит то, чего повтор уже не найдёт в `services.env`, и что установщик её проверяет.

## [3.0.0-beta.12] — 2026-08-30 · ветка `beta`

### Добавлено

- **Тесты на прерывание (`tests/test_interrupt.sh`).** Следующий класс после «команда упала»: операция умирает между двумя необратимыми действиями, и вопрос не в том, падает ли она, а в том — приведёт ли **повторный запуск** систему в согласованное состояние.

  Прерывание воспроизводится детерминированно, а не таймаутом: внешняя команда, которую операция зовёт по разу на шаг, подменяется заглушкой, выходящей с ошибкой на N-м вызове. Под `set -e` это и есть смерть посреди цикла.

  `regen-all` проверку прошёл: убитый после первого клиента из шести, он оставляет смешанное состояние (часть конфигов со старым профилем, часть с новым), повторный запуск приводит всех к одному, ключи клиентов не меняются, а третий прогон подряд не меняет ни байта.

- **Метка незавершённой миграции.** `do_migrate` пишет `MODE=parallel` **вторым** шагом, а восстановление ванильного редиректа — пятым. Смерть между ними оставляла «заявлено parallel, работа не доделана», и повторный запуск коротил на первой строке: «Уже режим parallel — миграция не нужна», код возврата 0. То есть работа не доделывалась **никогда**. Хуже: вместе с `MODE` терялся и старый режим, так что доделать было нечем даже при желании.

  Теперь перед первой необратимой записью кладётся метка со старым режимом, а снимается она последним шагом. Пока метка есть, операция считается незавершённой, и повторный запуск её доделывает. Переставлять `write_services` в конец не стал: между ним и концом функции двенадцать шагов, часть которых читает записанное, и проверить такую перестановку без живого сервера нечем.

  Отдельный пункт набора следит, что метка ставится **до** первой записи и снимается **после** всей работы: метка, поставленная позже, не прикрывает ничего.

### Исправлено

- **Матрица переходов мерила комментарий, а не код.** Она брала первое вхождение `if [ "$UPDATE" = 1 ]`, считая, что оно одно; с появлением `plan_report` таких строк стало три, переменная сделалась многострочной, и сравнение номеров превратилось в сравнение строк — «1007» оказалось меньше «849…». Теперь ветка берётся из `main()`, а поиск вызова игнорирует строки-комментарии.

## [3.0.0-beta.11] — 2026-08-30 · ветка `beta`

### Исправлено

- **Пустой публичный ключ стирал серверный конфиг.** Самое опасное из найденного, и найдено оно параллельным разбором уже после того, как аудит был написан. Цепочка: `expire_check` зовёт `del_client` левым операндом `||`, а в таком вызове `errexit` подавлен на **всё тело функции** — измерено. Значит отказ `awg pubkey` не останавливал удаление, а оставлял `cpub` пустым. Дальше фильтр в python ищет **вхождение подстроки**, а пустая входит в любой блок.

  Прогнал настоящий код на конфиге `[Interface] + 2×[Peer]`: **вырезаны все `[Peer]`** — все клиенты сервиса разом теряли доступ, при этом в журнале «Клиент удалён» и код возврата 0. Запускается это таймером, вывод уходит в `/dev/null`. Поставлено три рубежа: проверка кода возврата `awg pubkey`, проверка пустоты, и отказ в самом python — цена ошибки несоразмерна цене трёх строк. Плюс подоболочка в `expire_check`, чтобы `die` не обрывал весь прогон до записи обновлённого списка.

- **Ключ сервера в heredoc отдавал нерабочий профиль со словом «создан».** `PublicKey = $(server_pubkey)` внутри `cat > "$conf" <<EOF`: `die` внутри `$( )` убивает только подоболочку, `cat` дописывал `PublicKey = ` пустым, и клиент получал заведомо нерабочий конфиг. Ключ теперь берётся до рендера, обычным присваиванием с `|| die`.

  Отдельно: первая версия этой же правки была написана как `local spub; spub="$(…)"` и попала в тот же класс — измерено, что plain-присваивание с упавшей подстановкой под `set -e` роняет скрипт молча, и проверка на пустоту ниже становится недостижимой.

- **Приватный ключ сервера оставался в `/tmp` после каждого неудачного старта.** В датапасе отказавшая `awg setconf` запускалась второй раз уже внутри `then`, где `errexit` действует: `pipefail` проносил её код мимо нулевого `head`, скрипт умирал прямо там, и ни `rm -f "$stripped"`, ни `return 1` не выполнялись. А `$stripped` — это вывод `awg-quick strip`, то есть приватный ключ сервера, и юнит перезапускается по `Restart=on-failure`. Вывод теперь забирается с первой попытки.

- **Установка Go умирала на битом `go`.** `command -v go` подтверждает наличие файла, но не работоспособность: после оборванной прошлой установки `/usr/local/go` может быть битым, и тогда `ver="$(go version | awk …)"` под `pipefail` убивал установщик до строки «Установка Go», хотя пустой `ver` ниже уже трактуется как «не годится».

### Изменено

- **Аудит расширен на `awg` и `go`.** Обе находки выше он бы не поймал: в списке ненадёжных команд их не было. Теперь есть, и по итогам разобрано ещё одиннадцать мест — в основном законных (`_valid_key` это предикат, `$(go version …)` стоит в аргументе команды, а измерение показало, что отказ подстановки в аргументе вызывающего не роняет).

## [3.0.0-beta.10] — 2026-08-30 · ветка `beta`

### Добавлено

- **Автоаудит shell-паттернов (`tests/audit_shell_patterns.py`).** Классы ошибок, которые в проекте уже стреляли, теперь ищутся машиной, а не глазами. Проверяются пять: конвейер-предикат с `grep -q` (под `pipefail` SIGPIPE подменяет ответ на 141); ненадёжная команда в конвейере, чей код виден вызывающему (`grep`, `ls`, `find`, `git`, `getent`, `ip route get`); жёсткий номер колонки у `ss`; `${VAR:+…}` на переменной-флаге, принимающей «0»; `[ "$?" = N ]` после команды.

  Смысл не в запрете конструкций, а в том, что ни одно новое их появление не проходит молча. Каждое место обязано быть либо исправлено, либо внесено в `tests/audit_allowlist.txt` строкой с **письменным обоснованием**, почему именно здесь код возврата никуда не попадает. Список не гниёт: исключение, которому больше нечего прикрывать, аудит сообщает отдельно — проверено правкой прикрытой строки, после которой отчёт даёт сразу и новое место, и осиротевшее исключение.

  Правила намеренно узкие. Первая версия ловила `md5sum | cut` и `basename | sed` — шум, из-за которого список исключений стал бы свалкой вместо документа разбора. Осталось только то, для чего «ничего не нашлось» — штатный ненулевой код. Отдельно разведены два разных вреда: `pipe-fallible` про «скрипт умер», поэтому в условии `if` и при явном `|| …` он молчит; `pipe-predicate` про «ответ неверный», и там послаблений нет — именно `a | grep -q b || fallback` и объявлял занятый порт свободным.

### Исправлено

- **Установка Go не могла сообщить ни об одном своём отказе.** `want="$(curl … | head -1)"` и `sha="$(curl … | python3 …)"` — обе строки с заготовленными проверками ниже: «не удалось узнать версию Go» и «не нашёл sha256». Под `pipefail` отказ `curl` (нет сети, недоступен go.dev) убивал присваивание, и до этих сообщений дело не доходило никогда. Ровно тот класс, что уже находили в чтении ключа интерфейса.

- **`server_pubkey` умирала молча на ненастроенном сервере.** Ключ читался одной трубой прямо в `awg pubkey`, и `grep` без совпадения обрывал команду, не сказав, что в серверном конфиге нет `PrivateKey`.

- **Диагностика брала у `ss` жёсткую четвёртую колонку** — с комментарием, утверждавшим, что локальный адрес именно там. Это верно не для всех версий `iproute2`. Теперь предпоследнее поле, как и в `busy_ports`.

## [3.0.0-beta.9] — 2026-08-30 · ветка `beta`

### Исправлено

- **Генератор мог выдать `Jmin > Jmax`.** Инвариант восстанавливался ДО клампа по MTU, а кламп его же и ломал: при `mtu - 40 < Jmin` наружу уходила перевёрнутая пара. Воспроизводится на `--mtu 60 --preset paranoid` — `Jmin=24, Jmax=20`. Не отвергает такую пару никто: ни `awg`, ни netlink (там голые `NLA_U16` без сравнения), — а модуль берёт размер junk-пакета как `get_random_u32_inclusive(jmin, jmax)`, и этот хелпер требует `floor <= ceil`. Это тот самый вход, о котором [issue #225](https://github.com/amnezia-vpn/amneziawg-linux-kernel-module/issues/225) апстрима говорит как о переполнении кучи. Порядок переставлен: сначала потолок, затем инвариант.

  **Уже выданные конфиги это не трогает.** Пресеты описывают окно явными числами, самое широкое — 400, а `mtu - 40` при MTU 576 это уже 536: на всём рабочем диапазоне кламп до значений не дотягивается, и перевыпуск профиля даёт ровно то же окно, что и раньше. Проверяется отдельным пунктом набора `tests/test_junk_range.py`, который остаётся зелёным и на старом коде — краснеет только проверка инварианта на нерабочих MTU.

- **MTU проверяется на диапазон — но только там, где его вводит человек.** Раньше сверялось лишь «это число», и `--mtu 60` доезжал до генератора. Теперь установщик отказывается от значения вне 576-1500 (576 — минимум сборки IPv4, ниже туннель не поднимется ни у одного клиента; 1500 — несущий интерфейс, выше только фрагментация).

  Сознательно НЕ проверяется значение, приезжающее из `services.env`: оно попадает в `awg-obfuscation.sh` на каждом прогоне, включая повторное применение профиля. Отказ там сломал бы `--update` работающему серверу, у которого в состоянии лежит значение вне диапазона, а тихая подмена поменяла бы ему MTU и MTU в клиентских конфигах. Уже записанное не трогаем — инвариант генератор держит сам. Расстановка проверок закреплена отдельным пунктом набора.

## [3.0.0-beta.8] — 2026-08-30 · ветка `beta`

### Исправлено

- **Восстановление зашифрованного архива не работало в интерактиве вообще.** `openssl` читает пароль **из окружения** (`-pass env:BACKUP_PASS`), а `read` создаёт обычную переменную оболочки. На стороне создания архива `export` стоял, на стороне восстановления — нет: введённый с клавиатуры **верный** пароль давал `No environment variable BACKUP_PASS`, а с заглушённым stderr это выглядело как «неверный пароль или битый архив». Работал только путь `BACKUP_PASS=… awg-backup restore файл.enc`.

- **Миграция умирала на определении интерфейса — и второй попытки уже не было.** `difc="$(ip route get … | grep -oP 'dev \K\S+')"`: статус подстановки — это статус всего конвейера, и под `pipefail` он приезжает от `ip` (код 2 на хосте без маршрута по умолчанию). `set -e` убивал `do_migrate` прямо здесь — уже **после** того, как конфиги переименованы и в `services.env` лёг `MODE=parallel`. Повторный `--migrate` после этого коротит на «уже режим parallel — миграция не нужна», и ванильный редирект не восстанавливается никогда. Ветка `if [ -n "$difc" ]` ниже написана ровно на пустое значение и была недостижима.

- **Конфиг без `PrivateKey` убивал сборку интерфейса молча.** `_extract_key` и `_extract_key3` отдают код своего конвейера, а `grep` без совпадения — это 1. Присваивания стоят в теле `if`, маскировки нет: `build_iface` умирал, и заготовленные ветки «ключ отсутствует или битый» вместе с сообщением «сгенерирован новый ключ» были недостижимы именно тогда, когда нужны. Достижимо это состояние легко: конфиг перезаписывается усекающим редиректом, и прерывание в этом окне оставляет файл без строки `PrivateKey`.

- **Выдача клиента умирала на определении адреса.** `[ -n "$h" ] || h="$(ip route get … | grep -oP 'src \K\S+')"`: правый операнд `||` — последняя команда списка, поэтому `set -e` срабатывает на ней в полную силу. Отказ `ip` обрывал `awg-client add`, не доходя до запасного варианта через `api.ipify.org` ниже.

### Добавлено

- Четыре новых раздела в `tests/test_bash_traps.sh`: миграция на хосте без маршрута, оба чтения ключа интерфейса, определение адреса и пароль архива. Каждая защита проверена на покраснение.

## [3.0.0-beta.7] — 2026-08-30 · ветка `beta`

### Исправлено

- **Занятые порты не определялись вообще.** `busy_ports` и `port_busy` брали из `ss -lunH` поле `$5` — колонку `Peer`, то есть `0.0.0.0:*`. Она не заканчивается цифрой, поэтому список занятых портов был **пуст всегда**: измерено на живом `ss`, вывод пустой. Хуже того, `grep` без совпадения отдаёт 1, `pipefail` протаскивает это наружу, и сам вызов возвращал ненулевой код — под `set -e` он ронял присваивание `exclude=…`, в котором стоит первым.

  Номер поля тут вообще не та величина, на которую можно опираться: `ss` печатает колонку `Netid` не на всех версиях `iproute2` — при фильтре по одному протоколу она опускается. Теперь берётся **предпоследнее** поле: локальный адрес стоит там в любой раскладке. Проверено на обеих.

- **Занятый порт объявлялся свободным.** `printf '%s\n' $exclude | grep -qx "$p"` под `pipefail` отдаёт **141**, когда совпадение нашлось в начале длинного списка: `grep -q` выходит по первому совпадению, `printf` получает SIGPIPE. 141 ≠ 0 — срабатывала ветка `||`, и порт, найденный занятым, выдавался как свободный. На первой установке он закрепляется в `services.env` навсегда. Проверка переписана без трубы. Та же труба убрана из `ask_pick` (список коротких вариантов) и из `next_ip` в `client-awg.sh`, где иначе двум клиентам мог достаться один адрес.

- **Удаление клиента с испорченным конфигом умирало молча.** `del_client` читал ключ одной трубой прямо в `awg pubkey`. Конфиг без `PrivateKey` ронял всю команду без единой строки вывода: `grep` отдавал 1, и до сообщения дело не доходило. Теперь ключ читается отдельно, а причина называется вслух.

- **`plan_clients` считала конфиги через `find | wc -l`.** Под `pipefail` любой сбой обхода — подкаталог без прав, файл, удалённый по истечении срока прямо во время подсчёта — оборвал бы весь отчёт `--plan` между заголовком и первым разделом, и молча: сообщение `find` уже выброшено в `/dev/null`. То же исправлено в `awg3`.

### Добавлено

- `tests/test_bash_traps.sh` — набор на все четыре ловушки. Куски вырезаются из настоящих файлов и исполняются как есть, подставной `ss` проверяет **обе** раскладки колонок. Проверено, что набор краснеет: возврат `$5`, возврат трубы с `grep -qx` в каждом из трёх мест и возврат чтения ключа одной трубой роняют его каждый на своей проверке. Честно про охват: в `next_ip` список адресов /24 влезает в буфер трубы, поэтому поведенчески этот случай покраснеть не может — за него отвечает статическая проверка.

## [3.0.0-beta.6] — 2026-08-30 · ветка `beta`

### Добавлено

- **Сухой прогон `--plan`.** Перед запуском на работающем сервере вопрос всегда один: отвалятся ли клиенты. Ответ зависит от состояния сервера и набора флагов сразу — на машине с профилем обфускации обычный прогон его сохраняет, а `--reconfigure` выпускает новый и отключает всех, — и держать эту таблицу в голове владелец не обязан. Теперь `--plan` добавляется к той же команде, которую собирались запустить, и печатает её последствия: что останется как есть, что изменится, что сделано **не** будет, сколько выдано конфигов по каждому слою и нужен ли клиентам переимпорт. Работает со всеми флагами: `--plan --update`, `--plan --reconfigure`, `--plan --awg 3`.

  План берёт решение «сохранить профиль или выпустить новый» из **той же функции**, что и реальный прогон (`obf_mode`). Второй копии условия в проекте нет намеренно: расхождение отчёта с делом — единственный отказ, который хуже отсутствия отчёта, потому что ему верят. По той же причине план читает состояние из `services.env` и профилей обфускации и потому требует root: без него он увидел бы пустоту и уверенно показал «первая установка» на работающем сервере.

  Второе свойство — план не меняет на диске **ни байта**. Проверяется буквально: состав и контрольные суммы каталога сверяются до и после, в том числе для `--plan --reconfigure`. На этом же тесте нашлась настоящая ошибка ещё до выпуска: `plan_services` считала порты и подсети и тут же их записывала, так что план переписывал `services.env`. Вычисление и запись разделены, а в `write_services` добавлена страховка — попасть туда в режиме плана можно только по ошибке в коде, и вместо тихой записи она теперь громко отказывает.

  Ещё две находки при вычитке отчёта: строка про конфиги слоя 3.0 при повторном прогоне не печаталась вовсе, а счётчик конфигов пользовался `${LAYER3:+…}` — та же ловушка, что уже ловилась в `install.sh`: «0» тоже непустое, и на сервере без слоя 3.0 отчёт дописывал «3.0 — 0». Обе закрыты тестами, и обе проверены на покраснение.

  Третья находка — уже в `install.sh` и самая опасная: проверка `--plan` стояла в `main()` ниже веток `--update` и `--migrate`, поэтому `--plan --update` уходил в **настоящее обновление**, а по дороге ещё и вычищался устаревший `awg-resume` от старых версий установщика. Диспетчер плана перенесён в самое начало `main()`, до любых действий. Заодно `--plan --awg 3` перестал молча показывать слои из `install-state.env`: план идёт мимо диалога, и флаг до него не доходил.

  Операциям без отчёта план себя не подменяет: `--plan` рядом с `--uninstall`, `--install-base`, `--install-bot` или `--remove-bot` отказывается с кодом 2 и объясняет, что делать. Молча выполнить чужую операцию или молча проглотить флаг — оба исхода одинаково плохи: в первом случае пользователь получает работу вместо отчёта, во втором — отчёт не о том, что спрашивал.

  Набор `tests/test_plan.sh` подключён к `bash tests/run.sh` и CI. Порядок веток в `main()` проверяется по номерам строк: это ровно та ошибка, которую нельзя увидеть, вызывая функции по одной.

### Изменено

- **Матрица переходов и тест перехода с `main` берут `obf_mode` целиком**, а не вырезают условие построчно грепом. Выделение функции такую выборку сломало: греп вернул пустое, `eval` ничего не сделал, и матрица начала мерить собственную заглушку. Теперь пустая выборка — фатальная ошибка набора: тест, тихо проверяющий сам себя, опаснее отсутствующего.

## [3.0.0-beta.5] — 2026-08-27 · ветка `beta`

### Добавлено

- **Матрица переходов.** Установщик стал stateful, и последние ошибки шли одной и той же дорогой: правка создаёт новое состояние, а edge case вылезает в соседнем переходе. Ровно так `--reapply` породил два молчаливых отказа подряд — в пункте меню и в `--preset`. Тесты отдельных функций такое не ловят.

  `tests/test_transitions.sh` проверяет пары «состояние × действие» и инварианты каждого: что `--update` структурно не доходит до профиля и клиентов; какой режим выбирается из каждого состояния; что при повторном прогоне профиль и конфиги те же, а при `--reconfigure` профиль новый, но **identity цела** — ключи, порты, подсети и адреса не меняются ни при каком действии; что слои не задевают друг друга; что включение 3.0 не трогает 2.0 вовсе; и что `--reapply` без профиля отказывается с объяснением, а не выпускает новый молча.

  Условия выбора режима вырезаются из настоящего установщика, а сам режим отрабатывает на реальных `awg-obfuscation.sh` и `client-awg.sh` с путями во временном каталоге — расхождение кода и матрицы невозможно по построению. Проверено, что матрица краснеет: возврат «всегда `--apply`», потеря ветки про `--preset`, снятие защиты `--reapply` и удаление раннего `return` из `--update` роняют её каждый на своей проверке.

- **Приёмка переехала в репозиторий, появился CI.** Наборы писались по ходу работы и жили вне git — на машине автора: проверить изменение мог только он, а сторонний участник не имел ни одного способа убедиться, что ничего не сломал. Теперь они лежат в `tests/`, запускаются одной командой `bash tests/run.sh` и гоняются на каждый push и pull request.

  CI делает и статические проверки: запрет CRLF по всему дереву — на этом проект ломается раньше всего, — и `shellcheck` с планкой на уровне `warning`, которой дерево соответствует (замечаний ноль). Версия `shellcheck` зафиксирована: пакет дистрибутива обновился бы и покрасил сборку задним числом.

  Логи GitHub Actions открываются только после входа в аккаунт, поэтому упавший набор дублирует свой вывод в аннотацию — она публична и видна прямо в pull request. Первый же прогон CI на этом и окупился: нашёл настоящий дефект (`SC2066`) и поймал в приёмке проверку, которая зависела от гонки, а не от кода, — на раннере она вела себя иначе, чем локально.

  Наборы намеренно разбирают **настоящие файлы репозитория**: блоки вырезаются из скриптов на лету, обработчики бота вызываются как есть с подставным Telegram, а команда для фонового юнита не сверяется со строкой, а действительно запускается на подставных `awg-obfuscation`/`awg-client`. Проверено, что прогон краснеет: возврат старого конвейера `systemctl list-unit-files | grep -q` и возврат сравнения с веткой `main` роняют его именно на тех наборах, которые за это отвечают.

  В CI добавлен запрет CRLF по всему дереву — на этом проект ломается раньше всего, а установщик чистит `\r` только у того, что раскладывает сам.

### Исправлено

- **Обратная сторона `--reapply`: пути, которые просят новый профиль, его не получали.** Пункт меню «Обновить обфускацию (новый профиль с текущими настройками)» предупреждает, что конфиги придётся раздать заново, берёт подтверждение — и не выставлял `RECONFIGURE`, то есть после правки выше молча ничего не менял. Владелец соглашался отключить всех и не получал ничего. То же с явным `--preset X`: он минует меню, `RECONFIGURE` оставался нулём, новый пресет записывался в `install-state.env`, а `obfuscation.env` оставался прежним — и `awg-obfuscation --show` начинал врать про пресет. Оба пути теперь выпускают профиль, как и обещают.

  Защита от перевыпуска ценна ровно настолько, насколько работают пути, которые перевыпуск просят: иначе вместо «конфиги не ломаются» получается «профиль не меняется никогда», и ротация обфускации при блокировках тихо перестаёт работать.

- **Подсети слоя 2.0 закрепляются так же, как порты.** `AZ_SUBNET`/`VPN_SUBNET` пересчитывались из конфига ванильного AntiZapret на каждом полном прогоне. Пока ваниль стоит на своём диапазоне, результат тот же, но её переезд увёл бы сервер, а у всех выданных клиентов остался бы прежний `Address` — `regen-all` этого не чинит, он адреса не трогает. Теперь закреплённое значение из `services.env` сильнее вычисленного, как это давно сделано для портов.

- **Обновление слоя перевыпускало профиль обфускации и отключало всех клиентов.** `gen_obfuscation` всегда звал `awg-obfuscation.sh --apply`, а тот генерирует **новый случайный** профиль. То есть любой полный прогон установщика — переход с `main` на `beta`, повторная установка, автоматизация без терминала — обрывал связь всем выданным клиентам сразу. От этого спасал единственный узкий случай: пункт меню по умолчанию уводит в `--update`, где стоит ранний возврат до `gen_obfuscation`. Защитой это не назовёшь: достаточно было запустить установщик с любым флагом, минующим меню.

  Появился режим `--reapply`: существующий профиль раскладывается заново, значения не меняются. Новый профиль выпускается **только** по явному `--reconfigure`. Файл профиля проверяется для каждого слоя отдельно — общая проверка по чужому перевыпускала бы клиентов на пустом месте. В `awg3` такой режим был с самого начала; здесь его просто не было.

  README при этом советовал для перехода между ветками именно `--reconfigure` — то есть ровно тот путь, который всех и отключал. Совет исправлен.

- **`regen-all` не был идемпотентен.** Блок обфускации вставляется перед `[Peer]`, но хвостовые пустые строки секции `[Interface]` оставались, и после вычистки старых ключей их становилось на одну больше. Каждый следующий прогон добавлял ещё одну: содержательно конфиг не менялся, а файл — менялся, и владелец видел «конфиги пересозданы» там, где не изменилось ничего.

- **`regen-all` теперь говорит, скольким придётся переимпортировать.** Раньше он печатал по строке на каждый конфиг — на сотне людей сотни строк, из которых не следовало главного. Теперь в конце: `Конфиги клиентов: 87 без изменений — переимпорт не нужен`, а при настоящем изменении названы и число, и имена.

- Появился набор приёмки `tests/test_migration_main_to_beta.sh`: он прогоняет клиентский конфиг через обе версии слоя и сравнивает побайтно и файл, и ссылку `vpn://`, и проверяет, что профиль не перевыпускается. Проверено, что набор краснеет: возврат «всегда `--apply`» и возврат накопления пустой строки роняют его каждый на своей проверке.

- **Кнопка «Проверить обновления» горела вечно на установке с `beta`.** Ветку помнит `.layer-branch`, но её читал только `awg-upstream-check.sh` — а кнопка в боте зовёт другой путь кода, `az_setup_runner.py check-updates`, и тот сравнивал ревизию слоя с веткой `main`, зашитой в умолчание `_remote_head(repo, branch="main")`. На beta-сервере ревизии не совпадали никогда: бот сообщал об изменениях, обновление ничего не меняло, кнопка загоралась снова. Проверено на боевом сервере: `.layer-rev` и `refs/heads/beta` совпадали байт в байт, а бот всё равно показывал обновление.

  Теперь ветка берётся из `.layer-branch`, а при его отсутствии восстанавливается из `AWG_INSTALL_SH_URL` в `bot.env` и записывается на диск — как и в `awg-upstream-check.sh`.

- **Бот не объяснял, почему приложение пишет «version 3.1».** Слой работает на AmneziaWG 3.0 (`amneziawg-go v3.0.20260805`), и 3.1 не используется намеренно, но приложение Amnezia с версии 5.0 считает версию протокола само и знает ровно одну тройку — 3.1; строки «3.0» в нём нет вовсе, а любой непустой параметр слоя 3 (у нас это `HeaderProtectionKey`) сразу даёт «version 3.1». В README это было описано, а человек, получивший конфиг и открывший приложение, видел расхождение без объяснения. Теперь пояснение приходит вместе с конфигом слоя 3.0 — там, где расхождение и встречается. Подписи слоя намеренно **не** переименованы: они соответствуют тому, что реально работает.

- **`--apply` возвращал 0, даже когда профиль не доехал до туннеля.** Вызывающий — бот, установщик, `&&` в командной строке — спокойно шёл дальше и раздавал клиентам конфиги от профиля, которого на сервере нет. Ровно это и произошло у владельца вручную. Теперь такой исход возвращает код 3: «в файлы легло, до работающего туннеля не доехало».

  Ругаться при этом можно только там, где есть чему ломаться. На первой установке юнитов ещё нет — их ставит `switch_services` уже **после** обфускации, — интерфейсы не подняты, и молчание здесь норма: скрипт спокойно сообщает, что профиль применится при первом старте, и возвращает 0. Установщики код 3 переносят (сервисы поднимаются строчкой ниже), а любой другой ненулевой код по-прежнему валит установку.

- **Бот определял исход операции по хвосту лога.** `regen-all` печатает по строке на каждый клиентский конфиг — на сотне человек это сотни строк, и предупреждения обфускатора гарантированно выпадали из окна. Случай «юнит не найден» не проверялся вовсе, а сообщение об ошибке уверяло, что «конфиги клиентов НЕ трогались», хотя интерфейсы к тому моменту могли быть уже перезапущены. Теперь юнит пишет исход в отдельный файл, и различаются четыре состояния: применено; применено, но демон не подтвердил параметры 3.0; сервер обновлён, а `regen-all` упал (клиенты без связи — сказано прямо, с командой для повтора); профиль не применён вовсе.

- **Кнопка перевыпуска сигнатур отключала всех без единого вопроса.** Она зовёт `--regenerate`, а тот сам ставит `APPLY=1` и перезапускает интерфейсы — радиус поражения тот же, что у смены пресета. Работала синхронно в обработчике callback (бот на минуты становился глухим, Telegram обрывал запрос), с таймаутом 180 секунд на `regen-all`, который на сотне клиентов не укладывается, и с отброшенным кодом возврата. Теперь у неё тот же экран последствий и тот же фоновый юнит.

- **Смена профиля из бота стирала домен и MTU.** Команда собиралась без `--regenerate`, поэтому `host`, `mtu` и `fp` брались из умолчаний скрипта, а не из сохранённого профиля: после первой же смены пресета `META_HOST` пустел, а `META_MTU` обнулялся навсегда. Теперь `--regenerate` передаётся всегда — явный `--preset` его перекрывает, ради этого и делался приоритет флагов.

- **Счётчик «затронуто людей» показывал ноль при ошибке.** `awg-client list` мог упасть, а бот отбрасывал код возврата и выводил успокоительный `0` ровно тогда, когда со слоем что-то не так. Теперь неизвестное число так и называется.

- **README обещал Debian 12.** Планку задаёт не слой, а сам AntiZapret: его `setup.sh` читает версию через `lsb_release` и выходит с кодом 5 на Debian младше 13 и с кодом 6 на Ubuntu младше 24. То есть на Debian 12 базы просто не будет, и слою не на чем работать — обещание было невыполнимым. Значок и раздел «Требования» приведены к Debian 13+, добавлено объяснение, откуда планка берётся. Комментарий в интеграции про «проверено на Debian 12/13» поправлен по той же причине.

## [3.0.0-beta.4] — 2026-08-26 · ветка `beta`

### Добавлено

- **Обфускация настраивается отдельно для слоя 2.0 и слоя 3.0.** Раньше пресет спрашивался один раз и применялся к обоим, хотя «интенсивность» значит для них разное: `router` и `low` объявлены без header protection, паддинга и таймингов, и на них слой 3.0 отдаёт ровно то же, что 2.0 — отдельный слой теряет смысл. Установщик теперь задаёт второй вопрос и **прямо предупреждает** об этом; полный набор 3.0 начинается с `medium`. Неинтерактивно: `--preset3 X` и `--template3 Y`.

  Совместимость сохранена: если значение не задано, слой 3.0 настраивается как 2.0 — именно так вели себя все прежние установки, и молча менять им профиль нельзя.

- **Порты слоя 3.0 можно задать вручную.** Раньше `--awg-ports` действовал только на 2.0, а порты 3.0 всегда выбирались случайно, и повлиять на это было нечем. Появился `--awg3-ports A,V`, а в интерактивной установке порты спрашиваются для каждого слоя. Приоритет: явно заданный сильнее закреплённого, закреплённый — сильнее случайного. Пересечение портов между слоями ловится сразу, а не всплывает потом как «Address already in use» на старте второго интерфейса.

- **Список клиентов стал постраничным** — по 10 на страницу, с навигацией и счётчиком «стр. N/M». По краям листается по кругу: с первой страницы «назад» уводит на последнюю, так до хвоста добираться одним нажатием. В подписи указано, сколько всего клиентов и какие из них показаны.


- **Список админов бота меняется без переустановки:** `install.sh --bot-admins 111,222,333`. Бот читает `AWG_BOT_ADMINS` один раз при старте процесса, поэтому достаточно переписать строку и перезапустить юнит — venv, зависимости и токен не трогаются. Работает на обеих исторических раскладках: и когда настройки лежат в `bot.env`, и когда они остались строками `Environment=` в самом юните. Список **заменяется целиком** — перечислять надо всех, включая себя.

  Значение проверяется до записи: пустая строка или мусор в списке отвергаются с кодом 2. Иначе бот стартовал бы с пустым `ADMINS` и не отвечал бы никому, включая владельца.

- **Смена пресета обфускации переехала в меню «🛡 Обфускация» и обзавелась подтверждением.** Раньше она пряталась в «🔄 Обновление», а кнопка «Сменить 2.0» в самом меню обфускации пресет не меняла — она лишь перевыпускала сигнатуры внутри прежнего. Теперь «🎲 Новые сигнатуры» названы своим именем, а рядом стоит «🛠 Сменить пресет».

  Перед применением показывается экран последствий: сколько человек потеряют доступ, что конфиги придётся раздать заново, что второй слой и порты не затрагиваются. Прежде выбор шаблона применял профиль **сразу** — одно случайное нажатие отключало всех клиентов слоя без единого вопроса. При выборе `router` или `low` для слоя 3.0 бот отдельно предупреждает, что на них слой вырождается в 2.0.

  Сама операция ушла в фоновый transient-юнит с живым логом: смена профиля перезапускает интерфейсы, а `regen-all` на сотне клиентов идёт минутами — в обработчике callback бот всё это время был глухим, а Telegram успевал оборвать запрос. Юнит заодно служит замком: второй админ получает отказ, а не гонку. `regen-all` выполняется **только** при успешной смене профиля, иначе клиентам раздались бы конфиги от профиля, который на сервер не лёг.

### Исправлено

- **Смена профиля обфускации не доезжала до работающего туннеля.** `awg-obfuscation --apply` печатал «Применено» и «Готово», но интерфейсы не перезапускал: наличие юнита проверялось конвейером `systemctl list-unit-files | grep -q`, а под `set -o pipefail` он возвращает 141. `grep -q` выходит по первому совпадению и закрывает читающий конец трубы, systemctl получает SIGPIPE на остатке вывода — и весь конвейер считается упавшим. `set -e` при этом молчит, потому что команда стоит в условии `if`.

  Для слоя 2.0 это означало, что сервер живёт на прежней обфускации, хотя в конфиге лежит новая. Для слоя 3.0 — хуже: его параметры (header protection, паддинг, тайминги) живут только в памяти `amneziawg-go` и применяются из `<iface>.v3` на `ExecStartPost`. Без перезапуска они не применялись вовсе, и диагностика честно писала «параметры 3.0 не применены», а владелец запускал команду снова и снова с тем же результатом.

  Срабатывало через раз, и не из-за объёма вывода: на замере (systemd 259, 344 юнита, 22 КБ — вчетверо меньше буфера трубы) гонку создаёт хвостовая строка «N unit files listed», которую systemctl пишет отдельно, уже после того как grep нашёл совпадение в 24-й строке и закрыл трубу. Ненулевой код выпадал в 31 прогоне из 60, условие оказывалось ложным в 34 из 60, а вычислялось оно дважды за запуск — так что «ни одной строки Перезапуск» это рядовой исход, а не экзотика. `--no-legend` и `grep -m1` проблему не снимают. Проверка наличия юнита теперь делается без конвейера (`systemctl cat`), а её отрицательная ветка **говорит вслух**, а не пропускает работу молча.

- **Диагностика спрашивала только один интерфейс слоя 3.0 из двух.** Проверялся `antizapret-awg3`; `vpn-awg3` про header protection не спрашивали никогда. Полный туннель мог сколько угодно работать как 2.0 — доктор об этом молчал, а в сумме с пропущенным перезапуском это и держало проблему невидимой. Теперь опрашиваются оба, и в отчёте видно, у какого именно ключа нет.

- **Диагностика не отличала «ключа нет» от «спросить не удалось».** Отсутствие `awg3-uapi.py`, мёртвый UAPI-сокет и честно пустой профиль давали один и тот же вердикт — владелец чинил не то. Теперь у каждой причины свой текст. Заодно убрана проверка бита `+x`: скрипт запускается через `python3`, и потерянное право на исполнение выдавало «параметры не применены» на исправном сервере.

- **Настройки слоя 3.0 не доходили до генератора обфускации.** В `gen_obfuscation3` комментарий стоял между `\` и командой, поэтому перенос строки рвался: префикс окружения `AWG3_AZ_CONF=… AWG3_VPN_CONF=…` превращался в обычное присваивание в текущей оболочке и до дочернего процесса не доходил. Спасали только совпадающие значения по умолчанию — при любом переименовании интерфейсов профиль лёг бы не туда. Заодно строка лога показывала пресет слоя 2.0 вместо выбранного для 3.0.

- **Добавлена проверка, что параметры 3.0 приняты демоном.** После перезапуска скрипт спрашивает их обратно через UAPI и, если `header_protection_key` не вернулся, пишет об этом прямо, с командой для разбора. Прежде расхождение «в файлах новое, в туннеле старое» снаружи было не видно совсем.

- **`--preset` при `--regenerate` молча игнорировался.** Команда вида `awg-obfuscation --v3 --preset medium --regenerate --apply` заканчивалась строкой `preset=low`: блок регенерации подгружал сохранённые метаданные и перезаписывал ими **все** переменные, включая только что разобранные из командной строки. Сменить пресет существующему слою было нечем — оставалось стирать `obfuscation3.meta` руками. Теперь разбор аргументов помечает то, что задано явно, и метаданные заполняют только незаданное. То же касается `--template`, `--fp`, `--host` и `--mtu`.

- **Проверка обновлений сравнивала beta-сервер с веткой `main`.** Ветку помнит файл `.layer-branch`, но установки, сделанные до его появления, его не имеют, а откат по умолчанию вёл на `main`. Расхождение веток вечное, поэтому «есть обновление» горело всегда, а нажатие кнопки ничего не меняло. Теперь при отсутствии файла ветка восстанавливается из `AWG_INSTALL_SH_URL` в `bot.env` — того самого URL, который кнопка «Обновить слой» и дёргает, — и сразу записывается на диск, чтобы чинить это один раз.

- **Бот называл слоем «AWG 2.0» то, что обновляет оба слоя.** Кнопка, заголовок, строка диагностики и оба финальных сообщения говорили про 2.0, хотя `install.sh --update` пересобирает и слой 3.0 тоже. Владелец слоя 3.0 не понимал, чем ему обновляться. Везде теперь «AWG 2.0/3».

- **Карточка клиента в боте показывала данные одного случайного интерфейса.** У части людей не было ни трафика, ни онлайна, ни IP, ни города с провайдером, ни истории подключений — при том что топ-5 по трафику в «Информации о сервере» их видел.

  Одно имя — это несколько peer'ов с разными ключами: у ванильного AntiZapret их сразу два (`antizapret` и `vpn`), у нашего слоя тоже бывает два. `client()` брала строку запросом `LIMIT 1` без сортировки, то есть первую по rowid, и дальше считала по ней **всё**: трафик, онлайн по её `last_handshake`, адрес и гео по её `endpoint`, историю подключений по её `pubkey`. Если человек ходил через полный VPN, а первой оказывалась запись `antizapret`, карточка показывала нули при живом трафике.

  С боевого сервера: у одного клиента `antizapret` даёт 0 МБ, `vpn` — 92.2 МБ, и **обе записи с handshake**. У другого порядок оказался удачным — поэтому у него всё отображалось, и баг выглядел выборочным. Топ-5 не страдал, потому что считает `JOIN`'ом по всем строкам сразу.

  Теперь карточка суммирует все интерфейсы клиента внутри слоя: трафик складывается, handshake берётся максимальный, адрес и слой — у самого свежего peer'а, история подключений объединяется по всем ключам. Добавлена разбивка по интерфейсам — без неё не видно, каким конфигом человек реально пользуется, а какой лежит мёртвым грузом. Слои при этом по-прежнему не смешиваются.

- **Пересозданный клиент показывал бы чужой трафик.** `del_client` не трогает `stats.db`, поэтому строка удалённого peer'а остаётся в базе навсегда. Само по себе это было безвредно, пока карточка брала одну строку, но суммирование сложило бы трафик нового клиента с трафиком его удалённого тёзки. Теперь в сумму идут только peer'ы, которые есть в серверных конфигах; если живых не осталось вовсе, история всё равно показывается — иначе она пропадала бы вместе с клиентом.

- **Диагностика пугала исправный сервер.** На пресетах `router` и `low` доктор писал «параметры 3.0 не применены — профиль работает как 2.0», хотя оба пресета объявлены с `header_protection=False`, `content_padding=None` и `timings=False`: ключа там нет **по замыслу**, а не из-за поломки. Владелец шёл искать несуществующую проблему.

  Теперь проверка смотрит на пресет из `obfuscation3.meta`. Для `router` и `low` это спокойная строка «так и задумано» с подсказкой, как перейти на полный набор 3.0. Для `medium`, `high` и `paranoid` отсутствие ключа — по-прежнему предупреждение, но теперь с указанием, где искать: `obfuscation3.env` (`AWG_HPK_HEX`) и файл `<iface>.v3`. Если метаданных нет вовсе, предупреждение тоже остаётся — молчать в неизвестной ситуации хуже.

- **Счётчик «👥 Клиентов» считал строки, а не людей.** `COUNT(*) FROM peers` — это записи, а у одного человека их несколько (ваниль заводит клиента сразу в `antizapret` и `vpn`), плюс удалённые клиенты остаются в базе навсегда: `del_client` её не чистит. На сервере с 85 людьми выходило 126. Теперь считаются люди, живущие в серверных конфигах, а число конфигов показывается рядом: `👥 Клиентов: 85 (126 конфигов) · онлайн 12`. «Онлайн» тоже стал считать людей, а не peer'ов — человек с двумя активными туннелями учитывался дважды.

- **Слой 3.0 мог не опрашиваться вовсе.** Если в `services.env` нет строки `LAYER3=` — а на установках, поднятых до появления слоя, её нет, — жёсткий дефault `"0"` молча выключал опрос: карточки 3.0 оставались пустыми навсегда, и понять причину было нельзя. Теперь при отсутствии строки слой определяется по наличию его конфигов; явное значение в `services.env` по-прежнему сильнее.

- **Часть клиентов не попадала в список бота.** Список рисовался одним экраном со срезом `[:80]`, и на сервере, где записей под девяносто, хвост отбрасывался молча — без единого намёка, что показано не всё.


- **`--install-bot` требовал заново вводить токен, чтобы добавить одного админа.** Прошлые значения искались только в systemd-юните, а установщик как раз выносит их оттуда в `bot.env` с правами 600 (юнит в `/etc/systemd/system` читается всеми). На такой установке `prev_token` и `prev_admins` всегда оказывались пустыми: обещание «Enter = оставить текущее» не работало, а Enter приводил к выходу с кодом 2. Теперь действующие настройки читаются по цепочке `bot.env` → `install-state.env` → юнит, и токен спрашивается, только если его действительно негде взять.

- Та же ошибка в `setup_bot()`: при переустановке слоя поверх бота, поставленного через `--install-bot`, значения не находились и бот молча пропускался.

- **Датапас слоя 3.0 не обновлялся никогда.** Мешали сразу два независимых блокиратора: `install_awg3()` выходила по проверке «бинарник на месте», не сверяя версию, а из ветки `--update` она вообще не вызывалась. В сумме `amneziawg-go` навсегда оставался той версии, с которой сервер поставили, сколько бы раз ни запускали обновление. Теперь сверяется ревизия исходников, а `--update` пересобирает датапас и перезапускает юниты `awg3@`, если версия действительно сменилась.

- **Проверка обновлений сообщала о несуществующем обновлении бесконечно.** Установленная версия сравнивалась с последним тегом апстрима, а проект намеренно держится серии 3.0 — расхождение было бы всегда: бот шлёт уведомление, `--update` ничего не меняет (см. пункт выше), уведомление приходит снова. Теперь обновлением считается расхождение с пином, а свежий тег апстрима показывается отдельной справочной строкой. Версия `amneziawg-tools` показывается без пометки «есть обновление»: они ставятся пакетом из PPA вместе с ванильным AntiZapret и отдельно от него не обновляются.

- Переменная `AWG_TOOLS_REF` объявлялась, но не использовалась нигде — читатель считал, что версия утилит зафиксирована, хотя это было не так. Убрана вместе с объяснением, откуда утилиты берутся на самом деле.

### Исправлено в коде

- `awg-doctor` при найденном плейсхолдере советовал `awg-obfuscation --regenerate` без `--v3` — для конфига слоя 3.0 это перегенерировало бы чужой слой, оставив поломку на месте. Теперь скрипт различает, в каком слое нашёл плейсхолдер, и советует нужную команду вместе с `regen-all`.
- В шапке `awg3-datapath.sh` остался комментарий «kernel-модуль знает параметры лишь до 2.0» с отсылкой к HEAD от 11.06.2026. Параметры 3.0 модуль знает с 30.07.2026; причина, по которой слой остаётся на userspace, названа настоящая.

### Исправлено в документации

Проверены все 13 строк таблицы диагностики и остальной README — по коду проекта и по исходникам клиентского приложения (тег 5.0.1.5). Верными оказались четыре строки, остальные переписаны.

- **Совет при подписи «AmneziaWG Legacy» был перевёрнут.** README отправлял импортировать `vpn://` вместо `.conf`, «потому что при импорте файла приложение отбрасывает всё, кроме базовых параметров». Оба утверждения неверны: подпись ставится строго по отсутствию флага `isThirdPartyConfig`, а импорт `.conf` выставляет его сам — то есть Legacy может прийти **только** из ссылки. Параметров файл тоже не теряет: копируются все 25 ключей, включая семь v3. Настоящая причина — ссылка, собранная экспортёром до 3.0.0-beta.2.
- **Порог версии приложения назван конкретно:** 5.0.0.2. Раньше было «только свежие сборки», проверить это пользователь не мог.
- **Split-клиент.** Совет «раздай свежий конфиг» не работал: `AllowedIPs` фиксируется в момент создания клиента, и ни `regen-all`, ни кнопка «Скачать» его не обновляют. Нужно пересоздавать клиента.
- **Debian «not signed».** Совет `--update` не чинил поломку ни в одном состоянии машины: блок keyring лежит после досрочного выхода из `install_awg()`. Добавлен рабочий рецепт с `chmod 0644`.
- **Плейсхолдер обфускации** — их два, `__AWG_OBFUSCATION__` и `__AWG3_OBFUSCATION__`, и лечатся они разными командами. Прежний совет `--reconfigure` был самым тяжёлым из возможных.
- **Слой 3.0 не собирается.** Причина «нет Go/gcc» невозможна: установщик ставит их сам и падает раньше, чем появятся юниты. Перечислены реальные точки отказа датапаса.
- Уточнены: «не изменяются ни на байт» (DNS ванили всё-таки правится), состав кнопок бота, число интерфейсов в статистике, требования к чистому серверу, область действия `--awg-ports` и MTU (только слой 2.0), поведение портов при `--reconfigure`, что делает `awg-upstream-check`. В таблицу флагов добавлен `--uninstall`, исправлено описание `--bot-admins`. В консольный блок обфускации добавлен `awg-client regen-all` — без него сервер уезжает на новый профиль, а клиенты остаются на старом.
- **«Конфиги пересобираются автоматически» — только для бота и `--reconfigure`.** При ручном запуске `awg-obfuscation` из консоли клиенты не трогаются вовсе: сервер уезжает на новый профиль, а у всех клиентов тихо умирает handshake. Команды `awg-client regen-all` в README не было ни разу, хотя именно её зовут бот и установщик.
- **`--deep` проверяет только один слой** (2.0, а если он не поднят — 3.0), а не оба, как читалось раньше.
- **Портов не один, а два или четыре** (по одному на интерфейс), и вручную задаются только порты слоя 2.0.
- **Про удаление слоя 3.0 сказано честно:** `--awg 2` на установленной машине игнорируется, юниты `awg3@` остаются включёнными и переживают перезагрузку. Дан рабочий способ их погасить.

### Изменено

- `amneziawg-go` поднят с `v3.0.1` до **`v3.0.20260805`** — последнего тега серии 3.0, где среди прочего исправлено игнорирование keepalive.

- На **AmneziaWG 3.1** (вышла 12.08.2026, добавляет `RandomTrailers` и `DisableCookies`) сознательно не переходим: [kmod #215](https://github.com/amnezia-vpn/amneziawg-linux-kernel-module/issues/215) и [amnezia-client #3043](https://github.com/amnezia-vpn/amnezia-client/issues/3043) описывают одно и то же — хендшейк проходит, трафик не идёт, — обе открыты без ответа мейнтейнеров, и #3043 прямо сообщает, что userspace 3.1 и kernel-модуль 3.1 ведут себя одинаково.

- Комментарий «kernel-модуль знает параметры только до 2.0» в коде и README устарел: параметры 3.0 модуль знает с 30.07.2026 (PR #192). Причина, по которой слой 3.0 остаётся на userspace, теперь названа настоящая — открытые регрессии и отсутствие чистого тега.

## [3.0.0-beta.3] — 2026-08-16 · ветка `beta`

### Исправлено

- **Создание клиента обрывалось, если конфиг не влезал в QR.** `awg-export.py` падал с `DataOverflowError` на split-туннеле: `AllowedIPs` собирается из `/etc/wireguard/ips` и вырастает далеко за потолок QR-кода (~2953 байта). Под `set -euo pipefail` ненулевой код экспортёра убивал `add_client` сразу после вызова, а peer к этому моменту уже был прописан и в рантайме, и в серверном конфиге — клиент оставался полусозданным. Теперь при переполнении пропускается только PNG: `.conf` и ссылка `vpn://` создаются всегда, предупреждение уходит в stderr, а `client-awg.sh` логирует QR лишь тогда, когда файл действительно появился. Исправление предложено @Kirito0098 (PR #1).

  У слоя 3.0 переполнение вероятнее, чем у 2.0: в `[Interface]` дополнительно попадают `HeaderProtectionKey`, `ContentPaddingAddition`, `RekeyAfterTime`, `RekeyTimeout`, `RejectAfterTime`, `KeepaliveTimeout` и `MaxHandshakeAttempts`.

- **Пользователю мог уехать QR от предыдущей генерации.** При пропуске QR старый `.png` оставался на диске, а признаком свежести и в `client-awg.sh` (`[ -f ]`), и в боте (`os.path.exists`) служит само наличие файла. `regen-all` и смена порта/подсети перегенерируют конфиги, не удаляя картинки, — клиент получал код со старыми ключами обфускации и старым портом: туннель не поднимался, а в логах всё выглядело успешным. Теперь целевой `.png` при пропуске удаляется, а запись идёт через временный файл, поэтому обрыв на полпути не оставляет битую картинку.

- **«Нет библиотеки QR» больше не выдаётся за «конфиг слишком большой».** `segno` ставится в системный python3 цепочкой, оканчивающейся на `|| true`, так что её отсутствие — штатно достижимое состояние. Раньше в этом случае владелец видел «conf слишком большой для QR» даже для полного туннеля с `AllowedIPs = 0.0.0.0/0` (~600 байт). Теперь это отдельная ошибка окружения: экспортёр возвращает код `3` **после** записи `.conf` и `.vpn`, благодаря чему снова работают фолбэк бота на системный python3 и подсказка про `pip install segno`. Вызовы экспортёра в `client-awg.sh` перестали быть смертельными под `set -euo pipefail`.

- **Уровень коррекции QR у обычных конфигов не понижается.** Перебор шёл от слабого уровня к сильному, хотя ёмкость QR максимальна как раз у слабого (версия 40: L=2953, M=2331, Q=1663, H=1273) — попытки после первой были заведомо мертвы, а сама первая позволяла выбрать символ меньшей версии с более слабой коррекцией. Порядок сокращён до `M → L`.

- **Переполнение в ветке `qrcode` не распознавалось.** Библиотека сообщает о нём не только `DataOverflowError`, но и голым `ValueError` («Invalid version (was 41, expected 1 to 40)») из подбора версии. Перехват расширен, но сужен по месту — только вокруг подбора: на записи такой же `ValueError` бросает уже PIL, и глушить его нельзя. Запись во временный файл ведётся с явным `kind="png"` / `format="PNG"`: формат обе библиотеки берут из расширения, а у временного файла оно `.tmp`.

- Бот объясняет, почему QR не пришёл, на обоих путях выдачи и не отправляет PNG нулевого размера — `send_photo` на таком файле ронял хендлер целиком, и диалог оставался висеть на «⏳ Создаю…».

## [3.0.0-beta.2] — 2026-07-26 · ветка `beta`

### Исправлено

- **Установка обрывалась на первой же строке: `: invalid option nameset: pipefail`.** Файлы попали в репозиторий с Windows-машины, где git отдаёт рабочую копию с CRLF, а загрузка через веб-интерфейс GitHub сохраняет их как есть — и `bash` читал `set -euo pipefail\r`. Добавлен `.gitattributes` c `eol=lf`: теперь переводы строк фиксированы и в индексе, и в рабочей копии, независимо от настроек git на конкретной машине. Всё дерево нормализовано. Установщик вдобавок вычищает `\r` из склонированного репозитория перед раскладкой, чтобы одна неудачная загрузка не ломала установку целиком.
- **Бот создавал клиента 2.0 вместо 3.0 и потом не мог отдать файлы.** Разбор аргументов `awg-client add` знал только сервисы `antizapret` и `vpn`, а всё незнакомое молча выбрасывал — запрос на `antizapret3` превращался в обычного клиента слоя 2.0 в чужом каталоге. Отсюда обе жалобы разом: файлы не приходили (бот искал их в `clients/antizapret3`, а лежали они в `clients/antizapret`) и в списке у таких клиентов не было пометки ³. Теперь сервисы слоя 3.0 распознаются, а неизвестный аргумент — это явная ошибка, а не тишина.
- **Сервер подписывался в приложении как «AmneziaWG Legacy»** даже при верной версии протокола. Слово «Legacy» приложение подставляет само, когда контейнер называется `amnezia-awg`, а флаг `isThirdPartyConfig` не выставлен: так оно отличает конфиги своего мастера установки от чужих. При импорте `.conf` приложение ставит флаг самостоятельно, а в ссылке `vpn://` его обязан проставить тот, кто ссылку собирает. Теперь подпись — «AmneziaWG (version 2)». Версии «3» приложение по-прежнему не знает: в его исходниках есть только 1.5 и 2, поэтому «version 2» — это максимум, который оно способно показать и для конфига 3.0. Параметры 3.0 при этом передаются и применяются.
- Самотест конфигов проверяет `isThirdPartyConfig` — эта же подпись больше не уедет к пользователю незамеченной.
- `awg-client add` для split-туннеля падал молча, если в системе нет `/etc/wireguard/ips`: под `set -e` подстановка возвращала 1, и скрипт умирал без единой строки в выводе.

- **Слой 3.0 не поднимался: `awg3@…` падал с «control process exited with error code».** Интерфейсы слоя 3.0 создаются с плейсхолдером `__AWG3_OBFUSCATION__`, а скрипт обфускации вырезал из конфига только вариант без тройки — плейсхолдер оставался в `[Interface]`, и `awg setconf` отказывался читать конфиг на нераспознанной строке. Так как `setconf` вызывается из `ExecStartPost`, systemd сообщал лишь о неудаче control-процесса, не показывая причину. Теперь плейсхолдер вырезается в обоих написаниях, датапас дополнительно отфильтровывает его сам и при отказе `setconf` пишет в журнал настоящую строку ошибки.
- **Установщик печатал «Слой 3.0 поднят» сразу после сообщения о том, что он не стартовал.** Итог теперь соответствует факту, а при неудаче в вывод попадают последние строки журнала каждого юнита.
- **Диагностика в боте приезжала сырым текстом** — с ANSI-раскраской, которая в Telegram превращается в мусор вида `[1;32m`, и без структуры. Бот берёт у `awg-doctor` машинный вывод и рисует сам: разделы, статусы кружками, итог отдельной строкой. Так же оформлен и самотест конфигов.
- **Ложное замечание про DNS.** Проверка требовала `view:addr` для подсетей полного VPN, хотя ванильный AntiZapret их для себя не заводит — вставлять было некуда, а `awg-doctor` каждый раз ругался. Теперь свой view спрашивается только там, где он есть у ванили.
- **DNS-view для подсетей слоя 3.0 не добавлялись** — `awg-knot-view.sh` знал только подсети слоя 2.0, поэтому `awg-doctor` постоянно сообщал, что в `kresd.conf` не хватает view для двух подсетей. Теперь скрипт обрабатывает подсети обоих слоёв.
- `awg-knot-view.sh` вызывается и при установке одного лишь слоя 3.0 (раньше — только вместе со слоем 2.0).
- Вывод `awg-doctor --json` снова содержит заголовки разделов, поэтому диагностика в боте рисуется с разбивкой, а не сплошным списком.
- Карточка клиента подписывает слой по его происхождению (2.0 / 3.0 / сток), а не всегда «AmneziaWG 2.0».
- `awg-selftest.py` находит экспортёр рядом с собой, а не только по пути установки: скрипт можно запускать прямо из репозитория.

## [3.0.0-beta.1] — 2026-07-26 · ветка `beta`

Ветка обкатки. Стабильная версия — `main`.

### Добавлено

- **Слой AmneziaWG 3.0** параллельно слою 2.0: интерфейсы `antizapret-awg3` / `vpn-awg3`, подсети со сдвигом третьего октета на +2, отдельный UDP-порт, отдельный профиль обфускации (`obfuscation3.env`).
  - датапас — userspace `amneziawg-go` (юнит `awg3@`), потому что kernel-модуль знает параметры только до 2.0, а `awg-quick` молча уходит в него;
  - параметры 3.0 (`HeaderProtectionKey`, `ContentPaddingAddition`, `RekeyAfterTime`, `RekeyTimeout`, `RejectAfterTime`, `KeepaliveTimeout`, `MaxHandshakeAttempts`) применяются через UAPI-сокет: `amneziawg-tools` их не парсит.
- **Выбор версии при установке** — `--awg 2|3|both`, по умолчанию установщик спрашивает.
- **Выбор версии в боте** — при создании обычного и временного клиента; при двух слоях появляется шаг «Версия протокола», при одном экраны остаются прежними.
- **Обфускация по слоям** в боте и в CLI (`awg-obfuscation --v3`), включая перенастройку пресета/мимикрии.
- **`awg-doctor`** — проверка по слоям (юниты, порты, интерфейсы, профили), `--deep` поднимает клиента в netns и проверяет реальный handshake, `--json` для мониторинга.
- **`awg-selftest.py`** — прогоняет `vpn://` через те же проверки, что делает приложение Amnezia при импорте.
- **`awg-upstream-check.sh`** — сообщает о новых версиях `amneziawg-go`, tools и слоя; ничего не обновляет сам.
- **`awg-backup backup --encrypt`** — AES-256 + PBKDF2; `restore` распознаёт `.enc` автоматически.
- **Кнопка «🩺 Диагностика» в боте** — `awg-doctor`, глубокая проверка и самотест клиентских конфигов прямо из чата.
- **Клиентские профили попали в бэкап.** Приватные ключи клиентов лежат только в `/opt/antizapret-awg/clients`; без этого каталога восстановленный сервер работал, но все выданные конфиги пришлось бы создавать заново.
- **`allowed_ips` и `persistent_keep_alive` в `vpn://`** — режим туннеля теперь определяется однозначно, а не сравнением текста конфига со строкой-эталоном.

### Изменено

- Токен бота больше не хранится в юните (`/etc/systemd/system` читается всеми) — переехал в `/opt/antizapret-awg/bot.env` с правами `600`. Существующие установки мигрируются при `--update`.
- Статистика и карточка клиента различают слои: `🅾️` — 2.0, `🅾️³` — 3.0, `🅰️` — сток.
- `regen-all` пересобирает клиентов обоих слоёв, каждый со своим профилем.
- Восстановление из бэкапа перезапускает те интерфейсы, что записаны в `services.env`, а не жёстко `awg-quick@antizapret`/`@vpn` (в режиме `parallel` их зовут иначе).
- DNS-view в `kresd.conf` и самовосстановление после обновления AntiZapret охватывают подсети и юниты слоя 3.0.
- Установщик и бот в этой ветке работают с веткой `beta`: обновление не перескакивает на `main`.

## [2.1.1] — 2026-07-26

### Исправлено

- Приложение Amnezia VPN показывало импортированный конфиг как **«AmneziaWG Legacy»** (то есть 1.0/1.5) вместо 2.0. Причина не в самом конфиге: при импорте `vpn://` приложение не вычисляет версию протокола, а читает готовое поле `protocol_version` из контейнера, и пустое значение трактует как легаси. Теперь экспортёр проставляет версию по той же логике, что и импортёр клиента: есть `S3` и `S4` → `2`, иначе только I-пакеты → `1.5`. Поле влияет не только на подпись в интерфейсе — от него зависит, будет ли клиент подставлять свои дефолты для `S3`/`S4`.

  Исправление действует на ссылки, созданные **после** обновления. Для уже выданных клиентов ссылки пересобираются командой `awg-client regen-all` — после неё «📥 Скачать конфиг» в боте отдаст исправленную `vpn://`, которую нужно переимпортировать на устройстве. Ключи, порты и профиль обфускации при этом не меняются, поэтому работающие туннели переподключать не требуется — до переимпорта они просто продолжают показываться как «Legacy».

## [2.1.0] — 2026-07-16

### Добавлено

- **Подменю «Настройки AntiZapret»** в боте — управление штатными функциями GubernievS:
  - анти-цензурный патч OpenVPN (`patch-openvpn.sh`, режимы 0/1/2);
  - включение/выключение OpenVPN DCO (`openvpn-dco.sh`);
  - правка списков маршрутизации `include-hosts`, `exclude-hosts`, `include-ips` прямо из бота (добавление и удаление строк) с напоминанием обновить списки.
  Патч и DCO показываются только при установленном OpenVPN.
- **Проверка обновлений** (🔎): сравнивает код установщика AntiZapret и код слоя на GitHub с установленными версиями и подсказывает, нужно ли обновляться. Полное обновление AntiZapret выполняется штатной командой в терминале — бот показывает её. Списки блокировок в проверку не входят.
- **URI `vpn://` и QR для стокового WireGuard**: при создании и скачивании стокового клиента бот отдаёт для обоих туннелей обфусцированный конфиг, QR и ссылку для приложения Amnezia.
- **Полная статистика OpenVPN**: трафик, IP, город, провайдер и история подключений копятся в общей базе; в карточке клиента — активная сессия и накопленные данные.
- **Меню подстраивается под установку**: если AntiZapret поставлен без OpenVPN или без WireGuard, соответствующие пункты скрыты.

### Изменено

- После выдачи клиентских файлов бот заново открывает главное меню отдельным сообщением.
- Легенда в списке клиентов уточнена: раздельно обозначены split- и полнотуннельные клиенты слоя.

### Исправлено

- Бот не видел файлы стоковых и созданных при установке клиентов (имя файла строится по логике штатного `client.sh`).
- Статистика не находила клиентов, которые ещё ни разу не подключались.
- Перегенерация обфускации из бота не находила конфиги слоя в parallel-режиме.
- Debian 13: репозиторий Amnezia не подписывался из-за прав на keyring (теперь `0644`).
- Ответ «y» при установке через `bash <(curl …)` иногда игнорировался — ввод переведён на управляющий терминал, ответы нормализуются.

## [2.0.0] — 2026-07-14

### Добавлено

- **Параллельный режим по умолчанию.** Слой AmneziaWG 2.0 работает поверх штатного AntiZapret, не изменяя его: свои интерфейсы `antizapret-awg`/`vpn-awg`, свои подсети и UDP-порт. Совместимость со сторонними админ-панелями без патчей.
- **Рандомный UDP-порт с закреплением**, исключающий зарезервированные AntiZapret порты; ручное задание через `--awg-ports`.
- **Управление стоковыми WireGuard/AmneziaWG-клиентами из бота** — создание, удаление, список (через `client.sh` под `flock`).
- **Статистика по всем интерфейсам** — слой AWG 2.0 и стоковые клиенты учитываются раздельно; трафик, онлайн, история подключений с гео.
- **Кнопки обновления в боте**: обновление списков (`doall.sh`), обновление кода слоя (`install.sh --update`), перенастройка обфускации.
- **Доустановка и удаление бота** отдельными флагами: `--install-bot`, `--remove-bot`, `--bot-token`, `--bot-admins`.
- **Миграция со старых режимов** `replace`/`keep` на `parallel` через `--migrate` с сохранением ключей клиентов.
- **Установка базы с обходом GPG-бага** через `--install-base` (для случаев, когда официальный установщик падает из-за просроченного ключа OpenVPN).

### Изменено

- Режим работы теперь только `parallel`; прежние `replace`/`keep` объявлены устаревшими (путь миграции — `--migrate`).
- Самовосстановление слоя после обновления AntiZapret: в `parallel` штатный WireGuard не трогается, слой возвращается через drop-in и `awg-reintegrate.service`.
- Терминология в боте: «ванильный» → «стоковый».

## [1.0.0] — 2026-07-13

Первый публичный релиз.

### Добавлено

- Слой настоящего AmneziaWG 2.0 поверх AntiZapret с собственным транспортом и обфускацией (в ранних режимах `replace`/`keep`).
- Раздельная маршрутизация (AntiZapret) и полный VPN на AmneziaWG-слое.
- Настраиваемая обфускация: пресеты интенсивности, шаблоны мимикрии (QUIC/TLS/DNS/VoIP), выбор MTU и домена.
- Telegram-бот: управление клиентами AmneziaWG и OpenVPN, статистика, бэкап и восстановление.
- Клиентские артефакты: `.conf`, QR и ссылка `vpn://` для приложения Amnezia.
- Временные клиенты с автоудалением по TTL.
